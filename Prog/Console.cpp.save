#include <iostream>
#include <windows.h>
#include <string>
#include <stdio.h>

using namespace std;

int main()
{
    string a;
    getline(cin, a);
    //cout << a.size();
    for (int i = 0; i < a.size(); i++)
    {
        cout << (a[i]);
        Sleep(500)
    }
}
